<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:vm="clr-namespace:navtest.ViewModels"
			 Title="Scan for devices"
             x:Class="navtest.MainPage">

	<ContentPage.BindingContext>
		<vm:ScanViewModel></vm:ScanViewModel>
	</ContentPage.BindingContext>

	<Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
			<RowDefinition Height="*"></RowDefinition>
		</Grid.RowDefinitions>

		<Button Grid.Row="0" x:Name="btnScan" Text="Scan" Command="{Binding ScanCommand}" />

		<ListView Grid.Row="1" x:Name="lv" IsPullToRefreshEnabled="{Binding IsRefreshing}" ItemsSource="{Binding Items}"/>

	</Grid>

        </ContentPage>
